'use strict';

/**
 * Export endpoints for user.
 */

module.exports = [
    require('./create'),
    require('./read'),
    require('./update'),
    require('./delete'),
    require('./list'),
    require('./count')
];
