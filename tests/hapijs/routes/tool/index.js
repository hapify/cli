'use strict';

/**
 * Export endpoints for tool.
 */

module.exports = [
    require('./create'),
    require('./read'),
    require('./update'),
    require('./delete'),
    require('./list'),
    require('./count')
];
